# Progress

Append-only logg, nyeste øverst. Format: `- YYYY-MM-DD — hva ble endret og hvorfor`.

- 2026-07-04 — Byttet rosa aksent til eksakt hot pink `#FF69B4` (brukerens ønske) i stedet for den
  selvvalgte `#ff0066`. Hvit knappetekst på denne bakgrunnen gir kun ~2.65:1 kontrast (under AA selv for
  stor tekst) — bevisst latt stå siden ~45 steder i CSS-en hardkoder hvit tekst på `--accent`-bakgrunn,
  og en egen «tekst-på-aksent»-farge er en større omlegging enn det som ble spurt om. Notert i CSS-en.
- 2026-07-04 — **Rosa aksent** (ønske fra brukeren, til Sara). Ny personlig enhets-preferanse — «Mer»-
  menyen og Backup-panelet fikk en «Rosa aksent»-bryter ved siden av lys/mørk-bryteren, lagret i
  `localStorage` (`accent`, satt via `document.documentElement.dataset.accent`) akkurat som `theme`.
  Rosa er dreid til samme H/S/L-forhold som den blå aksenten (kun fargetonen endrer seg), og påvirker
  bare `--accent`-familien + `--surface-focus` i AppShell.css — kategori-swatchene (`SWATCH`) og
  innloggingsskjermens (hardkodede) blå hero-gradient er upåvirket. Verifisert i browser i lys+mørk
  modus.

- 2026-07-03 — **Delt kalender** (ønske fra brukeren). Hendelser kan nå merkes «Del med kjæresten» i
  hendelse-arket — gjenbruker samme delte Dexie Cloud-realm som «Delt»/«Handleliste» (ingen ny
  invitasjon nødvendig, alt deles allerede med samme person). `events` fikk et uindeksert `realmId`-felt
  (fraværende/`currentUserId` = privat); siden delte og private hendelser bor i samme tabell, dukker
  delte hendelser automatisk opp i kalenderen på begge enheter — ingen egen sammenslåingslogikk
  nødvendig. `isPrivateRealm` eksportert fra db.js for gjenbruk. Delte hendelser vises med et lite
  personer-ikon i dag-agendaen. Verifisert i browser (måtte simulere en faktisk avvikende realm-ID
  siden sandkassen mangler ekte Dexie Cloud-innlogging — «delt» og «privat» faller begge tilbake til
  samme `unauthorized`-verdi uten det).

- 2026-07-03 — **Fiks: ensidig sync i delte lister (manglet invitasjons-godkjenning)**. Etter forrige
  fiks (realm-oppslaget) kunne begge legge til, men bare den ene retningen synces: Saras varer dukket
  opp hos brukeren, men ikke omvendt — selv med samme realm-ID og «Synket»-status på begge. Rotårsak:
  Dexie Cloud krever et eksplisitt `invite.accept()`-kall før et medlemskap går fra «invitert» til
  aktivt (bekreftet ved at Saras medlemskap sto som «invitert» i del-panelet) — usynkede endringer
  pushes ut fint, men blir ikke *pushet ned* til et medlem som aldri har godtatt. Appen hadde aldri
  bygget UI for dette (`db.cloud.invites` ble ikke brukt noe sted). Ny `InviteBanner.jsx` — abonnerer på
  `db.cloud.invites`, viser Godta/Avslå-kort uansett hvilken fane man står på. Rammer «Delt»,
  «Handleliste» OG delte prosjekter likt, siden alle bruker samme medlemskapsmodell. Verifisert i
  browser med en simulert invitasjon (Godta-knappen kaller faktisk `invite.accept()`).
- 2026-07-03 — Lagt til Realm-ID i del-panelet (`SharedListView.jsx`) som feilsøkingshjelp — første steg
  i å spore opp den ensidige synk-bugen over.

- 2026-07-03 — **Fiks: kunne ikke legge til i «Delt»/«Handleliste»** (rapportert av brukeren rett etter
  at Handleliste ble skipa — men bugen var faktisk gammel, den traff aldri før). `ensureSharedRealm()`
  gjorde `db.realms.where({ name: SHARED_REALM_NAME })`, men dexie-cloud-addons `realms`-tabell
  indekserer kun `@realmId` — `name` er ikke en indeksert keyPath, så kallet kastet «KeyPath name on
  object store realms is not indexed» hver eneste gang (bekreftet via feilmelding brukeren fikk ved
  invitering, som bruker samme funksjon som å legge til). Fikset ved å hente alle realms og filtrere i
  minnet i stedet for en ugyldig indeksert `.where()`. Rammet både «Delt» og «Handleliste» likt siden
  begge går via samme funksjon. Verifisert i browser: legg-til fungerer nå (flere rekkefølge-kall også,
  altså både opprette og gjenfinne et eksisterende realm).

- 2026-07-03 — **Handleliste** (ønske fra brukeren, ikke fra roadmapen). Ny delt, avhukbar handleliste —
  gjenbruker hele delings-mekanismen fra «Delt» i stedet for å bygge en ny: `sharedItems` fikk et
  uindeksert `list`-felt (`'general'` default, eldre rader uten feltet regnes fortsatt som `'general'` —
  ingen migrering), og `listSharedItems`/`addSharedItem` tar nå en valgfri `list`-parameter. Selve
  UI-en er trukket ut til `SharedListView.jsx` (generisk, parametrisert på tittel/tomvisning/placeholder);
  `SharedList.jsx` («Delt») og nye `ShoppingList.jsx` («Handleliste») er tynne wrappere rundt den. Begge
  lister deler samme Dexie Cloud-realm/medlemmer — én e-postinvitasjon gir tilgang til begge (nevnt
  eksplisitt i del-panelets hint-tekst). Ny modul i nav (sekundær/«Mer»), eget `ShoppingCart`-ikon.
  Fant og fikset i samme slengen at «Delt» manglet i `HAS_COMPOSER` (viste både flytende «+»-knapp OG
  egen legg-til-linje samtidig) — lagt til «Handleliste» der òg. Verifisert med skjermbilder: filtrering
  riktig på begge lister, gamle rader uten `list`-felt havner riktig i «Delt».
- 2026-07-02 — **Fase 4D: Hage-interaktivitet** (fra produktgjennomgangen, siste punkt i fase 4). Trykk på
  en blomst eller et tre i Hagens SVG-scene viser nå en liten navnelapp (vanens/prosjektets navn) —
  samme trykk igjen, Escape, eller trykk utenfor lukker den. Kun i full visning (`GardenScene`s `compact`-
  modus på Oversikt-mini-kortet er uendret/ikke-interaktiv, siden hele kortet allerede er en lenke til
  Hage-fanen). Tastatur-tilgjengelig (`role="button"`, `tabIndex`, Enter/Mellomrom, `:focus-visible`-ring).
  Respekterer `prefers-reduced-motion` (tooltip-animasjonen slås av sammen med resten av scenen).
  Verifisert med skjermbilder av trykk/toggle/Escape/utenfor-trykk.
- 2026-07-02 — **Fase 4C: Kategori-swatcher inn i den blå familien** (fra produktgjennomgangen). Ny
  `src/lib/palette.js` med `SWATCH` — en muset pastell-fargefamilie (10 valører: blue/teal/forest/moss/
  amber/coral/rose/plum/violet/slate) avledet fra aksenten `#2f6dff` (samme HSL metning/lyshet ~S46/L48,
  ulik fargetone) i stedet for den gamle, tilfeldig sammensatte jordfargepaletten. Erstattet fargeverdiene
  (ikke nøklene — ingen migrering) i `PROJECT_COLORS` (projects/shared.jsx), `EVENT_COLORS` (Calendar.jsx),
  `CATEGORIES` (Money.jsx, som nå bruker alle 10 valørene), `HABIT_COLORS` (Habits.jsx) og Hagens
  `habitHex`-kart (Garden.jsx) — fant og fjernet tre kopier av samme gamle 6-fargers array. Ryddet også opp
  hardkodede hex på beslektede steder: `barColor()` i Money.jsx bruker nå `var(--danger)`/`var(--forest)`
  for over/innenfor budsjett, `.goal.reached` i Money.css bruker `var(--forest-soft)`/`var(--forest)`
  (fikser en reell mørk-modus-svakhet — lys grønn bakgrunn var hardkodet og tilpasset seg ikke), og
  Oversikt sitt Penger-kort bruker `SWATCH.blue` i stedet for en løs hex. Verifisert med lint+test+build
  og skjermbilder av Prosjekter/Kalender/Penger/Vaner.
- 2026-07-02 — **Fase 4B: Delte opp Projects.jsx/css og App.jsx** (fra produktgjennomgangen). `src/components/Projects.jsx` (1227 linjer) er nå en tynn ruter (liste ↔ arbeidsbenk); de faktiske komponentene bor i `src/components/projects/`: `shared.jsx` (konstanter/ikoner), `ProjectsList.jsx`, `StepSheet.jsx`, `SpineCard.jsx`, `WipLane.jsx`, `StageBlock.jsx`, `PromptQueue.jsx`, `PromptComposer.jsx`, `ShareSheet.jsx`, `Roadmap.jsx`. `Projects.css` (1773 linjer) delt tilsvarende i tematiske filer (`list.css`, `roadmap.css`, `workspace.css`, `prompts.css`, `composer.css`, `wip-result.css`), importert i original rekkefølge — bygget CSS-output var byte-for-byte identisk før/etter. `src/App.jsx` (835 linjer) delt: innloggingsskjermen flyttet til `src/components/Login.jsx` (+ delt `NavIcon.jsx`/`src/lib/icons.js` for modul-ikoner brukt av både nav og login), sky-sync/backup-panelet til `src/components/BackupSheet.jsx` (+ delt `src/lib/sync.js` for `syncLabel`/`syncLed`), ned til 476 linjer. Fant og fikset en reell krasj-bug underveis (spilt inn av skjermbilde-verifiseringen): `itemCount`/`led` ble brukt i en `useEffect`-dependency-array før de var deklarert (`const`-TDZ) — appen krasjet på hver innlasting, fanget av `ErrorBoundary`. Flyttet deklarasjonene tidligere i `App()`. Verifisert med lint+test+build (55/55 grønt) og skjermbilder av liste/roadmap/StepSheet/PromptQueue/PromptComposer/ShareSheet/mobil/innlogging.
- 2026-07-02 — **Fase 4A: Testgrunnmur + CI** (fra produktgjennomgangen). La til Vitest og skrev 55 enhetstester for de mest skjøre, rene funksjonene i appen — det som lettest knekkes stille under AI-drevne endringer. Trakk samtidig ut tre rene moduler fra `db.js`/`Projects.jsx` (ren bonus — gjør dem testbare OG er et forskudd på fase 4B): `src/lib/dates.js` (`todayKey`/`tomorrowKey`/`nextDate`, re-eksportert fra `db.js` for uendrede kall-steder), `src/lib/migrations.js` (`legacyTodoToTask` — delt av v10-migreringen og `importAll`s eldre-backup-mapping, som før var to kopier av samme logikk), `src/lib/prompts.js` (`projectContext`/`buildPrompt`/`buildAllPrompts`/`hasContext`, importert av `Projects.jsx`). Ny `.github/workflows/ci.yml` kjører lint + test + build på hver push/PR. `npm run test` lagt til i `package.json`. Lint + bygg + alle 55 tester grønt.
- 2026-07-02 — **Fase 3: Sterke funksjoner** (fra produktgjennomgangen). (A) **Claude-løkka er lukket**: hvert prosjektsteg kan lagre `result` (svar/PR-lenke/notat) — redigeres i utvidet kort («Resultat / svar», autolagres, URL-er blir «↗ Åpne lenke»-chip), 📎-indikator på kort med resultat, og kø-modusen fikk «Lim inn resultat…» som lagrer + setter steget til «Bygd» i ett trykk; resultater er søkbare via ⌘K. `setItemStage`/`moveItemToStage` stempler `doneAt`. (B) **«Denne uka»** på Oversikt: rolige chips (✅ oppgaver gjort · 🌿 vane-avhukinger · 🧩 steg ferdig, uke fra mandag) — skjult ved null, ingen streaks. (C) **Ukeslutt**: søndagskort på Oversikt (én gang/uke via `ukeslutt:<ukestart>`): feirer ukas tall, ett-trykks «ta med til mandag» for forfalte, «Klar for ny uke ✓». (D) **Startpakke**: helt tom konto (itemCount 0 + synk i ro) får tilbud om eksempler (demo-prosjekt m/kontekst+steg, 3 oppgaver, 2 vaner — alt slettbart) eller blank start; `starterOffered`-flagg. Lint + bygg grønt; verifisert med skjermbilder (resultat-kort, kø-paste, uke-chips, Ukeslutt).

- 2026-07-02 — **Fase 2: Kjerneopplevelsen** (fra produktgjennomgangen). (A) **⌘K er nå én kommandopalett**: hurtiglagringen søker samtidig i alt du har (oppgaver/idéer/prosjekter/steg/hendelser/vaner/forbruk/abonnement) og viser topp-5 treff under «Fant dette» med markering — trykk hopper til modulen; Enter legger fortsatt til. Søkeindeksen bor i `src/lib/search.jsx` (kjører kun når arket er åpent); den egne Søk-modulen + nav-fanen er fjernet (10 destinasjoner igjen). (B) **«I dag» snakker samme språk overalt**: Oversikt-ringen viser gjort/totalt («1/4 GJORT», ikke prosent) og første rad får samme aksent-«neste opp»-behandling som TodayHero; TodayHero fikk «💡 Foreslå noe fra listene mine» (åpner Hva nå?) når ingenting er planlagt. (C) **Tastatur-pass**: ny delt `useEscape`-hook — Escape lukker alle ark/overlays (StepSheet, PromptQueue, PromptComposer, ShareSheet, EventSheet, Expense/Budget/AmountSheet, Mer-arket, backup-panelet); oppgavetitler er tastaturtilgjengelige (role=button + Enter/Space). (D) **Backup-dytt**: eksport stempler `lastBackup`, panelet viser «Sist backup: …», og en mild toast (maks ukentlig) minner etter 30 dager — lukker ROADMAP-punktet. (E) **Bundle-diett**: Prosjekter lazy-lastes som Penger (egen 31KB-chunk; main 829→799KB — resten er rammeverk, tas ev. i fase 4). Lint + bygg grønt; verifisert med skjermbilder (palett med treff, Oversikt-ring, tom-dag-hero).

- 2026-07-02 — **Fase 1: Polér & fiks** (fra produktgjennomgangen). (A) Appen heter nå **Fabipilot** overalt (tittel, manifest, innlogging, sidemeny, backup-filnavn; Dexie-DB-navnet er bevisst uendret). (B) Ny `ErrorBoundary` (rot + rundt aktiv modul med `key=active`): krasj gir en rolig «dataene dine er trygge»-skjerm med Prøv igjen + Last ned backup i stedet for hvit skjerm; ny `ScreenSkeleton` erstatter blanke laste-glimt i Oppgaver/Prosjekt/Hage/Hva nå. (C) Kontrast til WCAG AA (`--muted` → #6b7280, `--faint` → #9096a3, mørke ekvivalenter justert, 10px-etiketter → 11px) + renset ~45 gjenglemte hex-farger fra den gamle varme paletten til tokens — inkludert **hele innloggingsskjermen som fortsatt var grønn/rav** (nå blå gradient); slettet død roadmap-spine-CSS; mørk modus QA-et med skjermbilder. (D) Fjernet død `todos`-kode (CRUD-hjelpere, Søk-spørring, itemCount) — migrering + backup-import beholdt. (E) PWA-oppdatering er nå `prompt` med sonner-toast («Ny versjon klar ✨ → Oppdater»), og sync-status vises: prikk på merkenavn/Mer-knappen ved feil/frakoblet + én feiltoast (nullstilles ved re-synk). Lint + bygg grønt; verifisert med skjermbilder (login, krasj-skjerm, skeleton, lys+mørk).

- 2026-07-01 — La til `SUPABASE-MIGRATION.md` — arbeidsnotat for en eventuell framtidig flytt fra Dexie Cloud til Supabase (skjema-mapping, auth, deling via RLS, tre synk-veier, faseplan, innsats/risiko). Ingen kodeendring — kun planlegging.
- 2026-07-01 — Deling av hele prosjekter via e-post (Dexie Cloud realms), etter samme mønster som den delte lista. Nye db-hjelpere: `shareProject(id,email)` lager et realm for prosjektet, flytter prosjektet + alle `projectItems` dit (`realmId`) og legger til et `db.members`-invite (`permissions:{manage:'*'}`); `listProjectMembers`, `removeProjectMember`, `stopSharingProject` (flytter tilbake til privat realm + fjerner de andre). `addProjectItem` arver prosjektets `realmId` så nye steg i et delt prosjekt også synces. UI: «Del»-knapp (ikon) i prosjekt-topplinja åpner et `ShareSheet` (e-postinvitasjon + medlemsliste + «Slutt å dele»), gjenbruker de delte `.share-*`-stilene. Prosjekter delt MED deg dukker automatisk opp i lista di via sky-synk. Selve sky-synken kan bare verifiseres innlogget på to kontoer — UI + logikk verifisert lokalt (arket åpnes, felt/knapper), lint + bygg grønt.
- 2026-07-01 — «Pågående»-lane på prosjekttavla — en egen full-bredde-sone øverst (over Høy/Medium/Lav) som viser det du fikser akkurat nå. Nytt `wip`-flagg på `projectItems` (uindeksert, ingen schema-bump). Sett som pågående: dra steget inn i lanen (`onDropWip` → `wip:true`) eller veksle via ⋯-arket («Sett som pågående»/«Ta ut»); ut igjen: dra tilbake til en prioritetskolonne (`onDropTo` setter `wip:false` + stage). WIP-steg trekkes ut av prioritetskolonnene og legges først i «Kjør prompts»-køen/«Kopier alle». Ny `WipLane`-komponent + `.rm-wip`-stil (blå-tonet, dropp-sone, kort-grid på PC). Lint + bygg grønt; verifisert PC + mobil + faktisk drag (Medium→Pågående, teller oppdatert).
- 2026-07-01 — Stort, alltid-synlig skrivefelt øverst på prosjekttavla (`.rm-quickadd`) for å gjøre det lettere å dumpe ideer/prompts inn i prosjektet. Textarea + «Legg til»-knapp: Enter legger til (i Medium), Shift+Enter = ny linje, og lim inn flere linjer → hvert linjeskift blir et eget steg (`addQuick` splitter på `\n`). Mal-chipsene flyttet under som «eller mal». Lint + bygg grønt; verifisert med skjermbilder + faktisk innlegging (PC + mobil).
- 2026-07-01 — Prompt-teksten som kopieres til Claude er nå på engelsk (brukeren jobber på engelsk), mens app-UI-et forblir norsk. Endret `projectContext` (Project/Goal/Context/Live/Repo), `buildPrompt` («Task:»), `buildAllPrompts` («Here's everything I want you to do:») og mal-`build()`-utdata (Build/Fix this bug/Improve the design/Add/Refactor…). Felt-etiketter, placeholders, knapper og toasts er fortsatt norske. Verifisert med faktisk utklippstavle-innhold (kopier alle + enkelt steg).
- 2026-07-01 — «Kopier alle»-knapp på prosjektsiden ved siden av «Kjør prompts»: kopierer alle stegene (køen: Høy→Medium→Lav) som én nummerert liste med prosjektkontekst foran («Prosjekt/Mål/Kontekst/Live/Repo» + «Her er alt jeg vil at du skal gjøre:\n1. …\n2. …») — klar til å lime inn i Claude som én batch. Delte ut `projectContext()` fra `buildPrompt` og la til `buildAllPrompts()`. Knappene ligger i en `.phead-run`-wrapper (full-bredde-rad på mobil, inline på PC). Lint + bygg grønt; verifisert med skjermbilder + faktisk utklippstavle-innhold.
- 2026-07-01 — Fjernet WIP-taket på aktive prosjekter (brukeren bruker prosjektene som Claude-prompt-verksteder for flere nettsider, så «maks 3 aktive» var i veien). `setProjectStatus` blokkerer ikke lenger, `addProject`/`promoteIdeaToProject` lager alltid aktive prosjekter, og labels viser bare antall («N aktive», «AKTIVE N») uten «/3». Fjernet `MAX_ACTIVE_PROJECTS` + tilhørende «WIP-tak nådd»/«lagt på is»-toasts (Projects + IdeaBank). «På is» finnes fortsatt som manuell status. Oppdatert produktprinsipp i CLAUDE.md §5. Lint + bygg grønt; verifisert med skjermbilde (6 aktive prosjekter uten tak).
- 2026-07-01 — «I dag»-panel øverst på Oppgaver (todolisten) for å gjøre dagen til hovedsaken. Ny `TodayHero` erstatter den enkle status-linja: fremdriftsring (gjort/totalt i dag), «X igjen · Y gjort» (eller «alt gjort»/«ingenting planlagt»), en ett-trykks «N henger igjen → ta med i dag» (redaterer alle forfalte til i dag via `setTaskDate`), og et «Neste opp»-felt som viser den viktigste neste tingen (første fokus/i dag/forfalt) med ett-trykks fullføring. Skjules i tom-tilstand. Resten av den seksjonerte lista er uendret under. Lint + bygg grønt; verifisert med skjermbilder + faktisk klikk (carry flyttet 2 forfalte → i dag, Neste opp fullførte og rullet videre).
- 2026-06-30 — Prompt-maler ble en ordentlig mal-komposer (`PromptComposer`) i stedet for chips som bare droppet en «…»-tekst. Malene har nå utfyllingsfelt med hint (`fields[]` + `build(v)`): Komponent, Fiks bug, Design, Ny funksjon, Refaktorer, Tom. Å trykke en mal-chip åpner et ark/dialog (bunn-ark mobil, sentrert på PC) med: mal-faner for å bytte type, enkle merkede felt (autofokus på første), live **forhåndsvisning** av den ferdige prompten, prioritetsvelger (Høy/Medium/Lav, default Medium), og «Legg til prompt» (deaktivert til alle felt er fylt). Mye lettere å si hva som skal gjøres enn å fikle med «…» i et lite felt. Lint + bygg grønt; verifisert med skjermbilder (PC-dialog + mobil-ark, med utfylling + forhåndsvisning).
- 2026-06-30 — Fjernet «Viktigst nå»-seksjonen (hero) på prosjektsiden — overflødig nå som tavla viser alt. Høy-kolonnen viser nå alle `now`-steg (`items={nowItems}` i stedet for `nowRest`); fjernet hero-JSX, ubrukt state/funksjoner (`editingHero`/`heroVal`/`heroCheckRef`, `startEditHero`/`saveHero`/`completeHero`, `hero`/`nowRest`) og imports (`useRef`, `burst`, `reduceMotion`, `CHECK`) + dead hero-CSS. Lint + bygg grønt; verifisert med skjermbilde (PC + mobil).
- 2026-06-30 — Prompt-verkstedet utvidet med maler + dra-og-slipp. (1) **Prompt-maler**: en `tpl-bar` over tavla med chips (`PROMPT_TEMPLATES`: Komponent/Fiks bug/Design/Refaktorer/Tester/Ytelse) — ett klikk legger et startsteg med mal-tekst i Medium og toaster «trykk for å fylle inn». (2) **Dra-og-slipp** mellom prioritetskolonnene (native HTML5 DnD, PC): `SpineCard` er `draggable` (av når man redigerer), bærer item-id i `dataTransfer`, blekner mens den dras; `StageBlock` er drop-sone (`onDragOver/Enter/Leave/Drop`) som lyser opp (`.drag-over` → accent) og kaller `onDropTo(stage,id)` → `setItemStage`. Mobil bruker fortsatt ⋯-arket for å flytte (touch har ikke native DnD). Lint + bygg grønt; verifisert med skjermbilde + faktisk drag (kort flyttet Medium→Høy, statistikk oppdatert).
- 2026-06-30 — Prosjektsiden ble et Claude-prompt-verksted (brukeren bruker prosjekt-stegene som en backlog av prompts hen skal kjøre i Claude/Codex senere). Fire ting, alle uten schema-bump (Dexie indekserer bare deklarerte kolonner): (1) **Kopier som prompt + kontekst** — `buildPrompt(project, text)` limer `Prosjekt/Mål/Kontekst/Live/Repo` foran stegteksten; kopier-knappen (steg + hero) bruker den og sier «Kopiert som prompt» når det finnes kontekst. Nytt `context`-felt på prosjektet (Claude-kontekst/stack, mini-CLAUDE.md) redigeres i info-skinna. (2) **Verksted-topp** — `liveUrl`/`repoUrl` på prosjektet vises som klikkbare ↗Live/⎇Repo-chips øverst i skinna (åpner ny fane), redigerbare inline. (3) **AI-status** — `aiStatus` per steg (`idea→asked→built→verified`, default `idea`) som en liten pille på hvert kort man trykker for å sykle; farges grå→blå omriss→blå fylt→grønn. (4) **Kø-/fokusmodus** — «▶ Kjør prompts N»-knapp åpner `PromptQueue`-overlay: én prompt om gangen (prioritert rekkefølge, øyeblikksbilde), «Åpne Claude ↗» (kopierer + åpner claude.ai/new), «Kopier prompt», «Hopp over» / «✓ Spurt — neste». Mobil-fiks: «Kjør prompts» flyttet til egen full-bredde-rad under tittelen (kolliderte med brutt tittel). Lint + bygg grønt; verifisert med skjermbilder (PC + mobil + kø).
- 2026-06-30 — Prosjekter som PC-arbeidsbenk. På store skjermer (≥860/1000px) bryter Prosjekter ut av 560px-kolonnen og bruker bredden: (1) Prosjektlista blir et responsivt kort-rutenett (`.plist-grid`, auto-fill minmax 310px) i stedet for én smal kolonne; sorter-pilene overlapper kortet og vises kun på hover (ingen layout-shift, korta ligger på linje med seksjonsetiketten); kort løfter seg på hover; «Nytt prosjekt»-baren holdes rolig bred (maks 620px, sentrert). (2) Prosjektsiden blir en to-spalte arbeidsbenk: en klistret info-skinne (`.rm-rail`: hvorfor, frist, notat, fremdrift, statistikk, «Viktigst nå») til venstre og en kanban-tavle (`.rm-board`) med Høy/Medium/Lav som tre kolonner side ved side til høyre; ferdig-seksjonen full bredde under. (3) Steg-korta (`SpineCard`) fikk handlingsknappene samlet i `.rm-actions` som på tavla legger seg i en egen bunnrad (tittel på toppen, kan brytes) — ellers stjal knappene all bredden i en smal kolonne. Mobil er uendret (`.plist-grid`/`.rm-workspace`/`.rm-actions` er `display:contents`/`display:flex` kun via media queries). Småfiks: hero-ramme rav→`--accent-soft`, steg-«+»-knapp skog→blå. Lint + bygg grønt; verifisert med skjermbilder (PC + mobil, liste + prosjektside).
- 2026-06-30 — Kopier-knapp på hvert prosjektsteg (`SpineCard` i `Projects.jsx`): liten outline-ikonknapp mellom delpunkt-chip og ⋯-menyen som kopierer stegets tekst til utklippstavla (`navigator.clipboard.writeText`) med vibrasjon + «Kopiert»-toast (feil → «Kunne ikke kopiere»). Ny `.rm-copy`-stil i `Projects.css` (tokens, blå hover). Lint + bygg grønt; verifisert med skjermbilde.
- 2026-06-30 — Ny visuell retning: Any.do-inspirert (rent, lyst, luftig) etter at brukeren mislikte den gamle, varme/myke profilen («ser amatør ut», «liker ikke fonten/layouten»). (1) Font: byttet Bricolage+Inter → Plus Jakarta Sans (variabel), selv-hostet via `@fontsource-variable/plus-jakarta-sans` i `main.jsx` (Google Fonts-CDN er proxy-blokkert + fungerer ikke offline i PWA). Fjernet Google Fonts-`<link>` i `index.html`. (2) Palett: skrev om `:root`-tokens i `AppShell.css` til lys (hvit lerret/kort, blekk `#1a1c22`, linje `#ecedf1`) + grafitt mørk modus; ÉN blå aksent `#2f6dff` for fokus/CTA/valgt/lenker, grønn `#16a37b` kun som positiv status. Fjernet de varme radial-gradientene på `.app`. (3) Layout: Oppgaver bygd om til rene rader med tynn skillelinje (ikke tunge kort) — kollapsede rader som utvider seg ved trykk på tittel; rolige grå seksjonsetiketter uten bokser. (4) Ryddet hardkodet gammel blekk i `index.css`; Kalender-krom (I dag-knapp, +Hendelse-CTA, oppgave-tag/prikker) flyttet fra skog til blå aksent; tema-farge i `index.html`/`vite.config.js`/manifest → `#2f6dff`. Lint + bygg grønt; verifisert med skjermbilder (Oppgaver, Oversikt, Penger, Kalender, Prosjekter, Vaner).
- 2026-06-30 — Oversikt «I dag»-kortet speiler nå den samlede lista: viser de neste 3 oppgavene (fokus først, m/ ⭐), med innebygd avhuking (sjekkbox → fullfør med gnist + vibrasjon, `stopPropagation` så kortet ikke navigerer) + «+N til». Faller tilbake til fremdriftsring + melding når tomt/alt gjort. Lint + bygg grønt; verifisert med skjermbilde.
- 2026-06-30 — ÉN samlet oppgaveliste (slo sammen «I dag» + «Liste»). Brukeren mislikte splitten. Datamodell: schema v10 migrerer `todos` → `tasks` (samme id-er, idempotent `bulkPut`, behold delpunkter/dato/fullført); `tasks.dueDate` kan nå være `null` (udatert); `addTask(title,{dueDate})`, `setTaskDate`, `setTaskDone` nullstiller ikke lenger datoen, task-delpunkter, `swapTaskOrder`; `importAll` mapper gamle `todos`-backup inn i `tasks`. Ny `Tasks.jsx`/`.css` (nav-nøkkel `today`, label «Oppgaver»): seksjoner Fokus/I dag/Henger igjen/Kommende (sammenleggbar)/Når som helst (sammenleggbar)/Fullført; naturlig-språk-innlegging med live dato-forhåndsvisning («handle melk i morgen» daterer seg selv) + «I dag/Når som helst»-velger for udaterte; smarte dato-chips (i dag/i morgen/til helga/velg/fjern) farget etter status; delpunkter m/ fremdrift; sveip (høyre=fullfør, venstre=utsett); fokus maks 3; «gjort nok»-linje. Fjernet `Today.jsx` + `TodoList.jsx`; oppdaterte `App.jsx` (én modul, ikke to), `Capture.jsx` (droppet «Liste»-chip, `todo`→oppgave). Lint + bygg grønt; verifisert med skjermbilder (liste, NL-forhåndsvisning, dato-meny).
- 2026-06-30 — Sveip på «I dag»-oppgaver (motion drag). Åpne oppgaver (ikke fullført/henger-igjen) kan sveipes: høyre = fullfør (gnist + vibrasjon), venstre = utsett til i morgen. Fargede handlingsfelt avsløres bak kortet (grønn «Fullfør» / skog «I morgen») med opacity som følger draget; snapper tilbake under terskel (`dragSnapToOrigin`, terskel 80px). `dragDirectionLock` + `touch-action: pan-y` så vertikal scrolling beholdes. Viktig fiks: `.task` sin `rise`-animasjon (`both`) beholdt `transform:none` og overstyrte motion sin drag-transform — skrudd av på `.task.swipeable`. Knappene (hak/måne) finnes fortsatt for ikke-sveip. Mekanikk verifisert med Playwright-pekerdrag (fullføring + avsløring); selve touch-følelsen må finjusteres på telefon. Lint + bygg grønt.
- 2026-06-30 — Hage-mini-kort på Oversikt. Refaktorerte `Garden.jsx` til å eksportere `useGardenData()` + `GardenScene` + `gardenCaption` (delt aggregering/scene). La til et live «Hagen din»-kort i Oversikt-rutenettet (fullbredde, lenker til Hage), så du ser hagen gro fra startsiden. Scenen `preserveAspectRatio="xMidYMax slice"` (forankret i bunnen så blomstene/vanene alltid vises); på PC sentrert maks 620px så fullbredde-kortet ikke blir en tynn stripe. Nytt kort havner bakerst i eksisterende `ovCards`-config (ikke-brytende). Lint + bygg grønt; verifisert mobil + PC.
- 2026-06-30 — «Hagen din» — signatur-/wow-skjerm (`Garden.jsx`/`.css`, ny modul i nav, sekundær). En rolig, levende SVG-scene som speiler uka og gir lyst til å åpne appen: vaner = blomster (størrelse etter 14-dagers jevnhet, blomstrer m/dugg hvis gjort i dag), aktive prosjekter = trær (oransje knopper hvis et «nå»-steg), oppgaver gjort i dag = sommerfugler, dagens fokus-% = solens varme/glød, penger på stell = klar himmel vs. en mild sky, himmel skifter med tid på døgnet (natt = stjerner). Mild bevegelse (svai/flagre/glitre), respekterer `prefers-reduced-motion`. Helt additiv — kun lesing over eksisterende stores, ingenting visner eller skammer (på linje med «ingen skam-streaks»). Tom-tilstand: én liten spire + «Plant noe i dag». Legende-chips lenker til Vaner/Prosjekter/I dag. Lint + bygg grønt; verifisert med skjermbilder (dag/mørk/tom).
- 2026-06-30 — Fjernet «halvferdig»-følelsen: ingen native nettleser-dialoger igjen. (1) Slett med «Angre»: nye hjelpere `deleteWithRestore`/`restoreRecord` i db.js; alle `window.confirm`-sletteflyter (Penger abonnement+sparemål, Vaner, Kalender-hendelse, Prosjekter steg+prosjekt) erstattet med direkte sletting + sonner-toast med «Angre» (prosjekt gjenoppretter også stegene; idé-sletting fikk også angre). Mindre angst, ingen bekreftelses-popup. (2) Alle `window.alert` → toast (Idébank «forfremm til prosjekt», Prosjekter WIP-tak ×2). (3) Alle `window.prompt` i Penger erstattet med et gjenbrukbart bunn-ark `AmountSheet` (stort tallfelt + kr-suffiks + Lagre): trekkdag, abonnementsbeløp, månedsinntekt, sparemål, +Spar/−. Lint + bygg grønt; verifisert med skjermbilder (ark + angre-toast).
- 2026-06-30 — Universell hurtiglagring + naturlig-språk-tolk. (1) `src/lib/parse.js`: `parseEntry(text)` tolker norsk dato/tid og typehint → `{title, type, dueDate, time}`. Forstår «i dag/i morgen/overmorgen», ukedager (+ «neste»), «om N dager/uker», «neste uke», «DD.MM», månedsnavn («15. juli»), klokkeslett («kl 14», «14:30», «kl 1430»), og prefiks «idé:/liste:/avtale:». Klokkeslett gjør en oppgave til hendelse. Renser matchede ord ut av tittelen. Enhetstestet mot 16 inputs. (2) `src/components/Capture.jsx` (+`.css`): bunn-ark med autofokus-felt, live forhåndsvisning (type + dato/tid-chips + renset tittel), type-overstyring (Oppgave/Liste/Idé/Hendelse), ruter til `addTask/addTodo/addIdea/addEvent`; bekreftelse via toast med «Vis»-snarvei. Nåbar overalt via ⌘K/Ctrl+K + en flytende «+»-knapp (`cap-fab`) på sider uten egen «legg til»-linje (`HAS_COMPOSER`-sett i App.jsx). Lint + bygg grønt; verifisert med skjermbilder (lys/mørk).
- 2026-06-30 — Daglig anker mot «jeg glemmer å åpne den». (1) Morgenrituale «Start dagen» (`MorningFlow.jsx`/`.css`) øverst på Oversikt én gang per dag (lagres `ritual:<dato>` i localStorage): hilsen, «henger igjen → ta med i dag», velg inntil 3 i fokus, avtaler + «igjen å bruke», «Sett i gang» (gnist + vibrasjon). (2) Påminnelser (`src/lib/notify.js`): «planlegg dagen»-varsel med valgbart tidspunkt + av/på-bryter + «test varsel» i innstillinger. Progressiv forbedring — Notification Triggers fyrer når appen er lukket der det støttes (Chrome/Android, installert PWA); iOS/Safari får in-app-nudge ved neste åpning + app-ikon-badge. Web Push droppet med vilje (krever server). (3) App-ikon-badge (`navigator.setAppBadge`) med antall ting igjen i dag. (4) «Du har gjort nok i dag»-linje på I dag når ≥3 er fullført (forsterker anti-overveldelse, ingen skam). Prefs lagres lokalt per enhet. Lint + bygg grønt; verifisert med skjermbilder (lys/mørk).
- 2026-06-29 — Premium UI-løft (design system + bevegelse). Fullt token-system i `AppShell.css` (`:root`): radius-, mellomrom-, elevasjons-/skygge-, bevegelses- og typografi-skala + raffinert lys/mørk-palett (alle gamle var-navn beholdt). Nye biblioteker: `motion` (sideoverganger, glidende aktiv-indikator i nav + Penger-faner via `layoutId`, reveal-stagger), `lucide-react` (konsistent ikonsett — erstattet håndtegnede nav-/Oversikt-/Penger-SVG-er), `sonner` (toast — erstattet `window.alert` i backup/sync), `recharts` (kakediagram for kategori-fordeling på Penger). Ny delt fil `src/lib/ui.jsx`: `AnimatedNumber` (myk tellende tall), `Skeleton`/`SkeletonCard`, `PageTransition`, `Reveal`, `toast`. Kort fikk hover-løft + hårfine kanter, knapper fikk hierarki (`btn-primary`), input-felt fikk fokus-glød. Penger lazy-lastes (recharts i egen chunk). Fikset en flex-bug der Idébank-filterchipsene kollapset (`min-width: max-content`). Respekterer `prefers-reduced-motion`. Verifisert med skjermbilder (lys/mørk, mobil/PC) + lint + bygg.
- 2026-06-29 — Bolk 3 (del 5): deling med kjæresten. Ny «Delt»-modul (`SharedList.jsx`) — én delt liste i ÉT Dexie Cloud-realm (ny `sharedItems`-store, schema v9). «Del»-panel inviterer på e-post (`inviteToShared` → `db.members` med full tilgang); medlemsliste m/ status. Helpere i db.js: `ensureSharedRealm`, `add/update/delete/setDoneSharedItem`, `listSharedMembers`, `removeSharedMember`. Kun denne lista deles — alt annet forblir privat; ikke med i lokal JSON-eksport. UI verifisert med skjermbilde; selve realm-/invitasjonsflyten må testes med to ekte innlogginger (kan ikke simuleres lokalt).
- 2026-06-29 — Bolk 3 (del 4): tilpassbar oversikt. «Tilpass»-knapp på Oversikt slår på redigeringsmodus der hvert kort kan flyttes opp/ned (▲▼) eller skjules; skjulte kort vises som chips man kan hente tilbake. Rekkefølge + skjulte lagres i `localStorage` (`ovCards`). Det første kortet beholder full bredde. Rent frontend, ingen data/schema.
- 2026-06-29 — Bolk 3 (del 3): mørk modus. Tema-tokens (`--canvas`/`--surface`/`--ink` osv.) overstyres under `[data-theme='dark']` på `documentElement`; valg lagres i `localStorage` og faller tilbake til `prefers-color-scheme`. Bryter i «Mer»-sheeten (mobil) + i backup/innstillinger-modalen (PC). Erstattet hardkodede `#fff`/`#faf4e8`/`#e9ece5`-bakgrunner med tokens på tvers av CSS-filene så hele appen følger temaet.
- 2026-06-29 — Bolk 3 (del 2): søk på tvers av moduler. Ny «Søk»-modul (`Search.jsx`) med ett søkefelt som matcher oppgaver, gjøremål, idéer (+ tagger), prosjekter, prosjektsteg, hendelser, vaner, forbruk og abonnement. Treff vises med emoji + uthevet (`<mark>`) tekst + type-tagg, og lenker rett til modulen via `onNav`. Rent lese-søk over eksisterende stores — ingen ny data eller schema. Ligger i «Mer»/sidemeny.
- 2026-06-29 — Bolk 3 (del 1): forbruks-trend på Penger/Oversikt. Søylediagram «Forbruk siste 6 måneder» (forbruk + faste per måned, regnet fra eksisterende `expenses` + `subscriptions` — ingen ny data eller schema). Valgt måned uthevet i rav; trykk på en søyle hopper til den måneden. Skjules når alt er null.
- 2026-06-29 — Bolk 2 (del 4, siste): ukemål for vaner (uindeksert `weeklyGoal`-felt på `habits`, «Ukemål»-velger Av/2x–6x i customize-panel, fremdriftslinje «N/M denne uka» i vanefarge) og delsteg i prosjektsteg (uindeksert `subtasks`-array på `projectItems` + `addItemSubtask`/`toggleItemSubtask`/`deleteItemSubtask`, ☑ N/M-chip som utvider en mini-sjekkliste under steget).
- 2026-06-29 — Bolk 2 (del 3): gjentakende oppgaver (uindeksert `repeat`-felt none/daily/weekly; `setTaskDone` lager neste forekomst når en gjentakende oppgave hukes av; kompakt ↻-chip i I dag) og gjentakende hendelser (uindeksert `repeat` none/daily/weekly/monthly; kalenderen ekspanderer forekomster via `occursOn`, ↻-merke i agendaen, «Gjentar»-velger i hendelse-sheet).
- 2026-06-29 — Bolk 2 (del 2): tidsestimat på oppgaver (uindeksert `estimate`-felt, chip i I dag som sykler 5/15/30/60/av), «Jeg har X minutter»-filter i Hva nå? (filtrerer oppgaver på estimat), og pomodoro/fokus-timer i Hva nå? (15/25/45 min, start/pause/nullstill, gnist + vibrasjon når ferdig).
- 2026-06-29 — Bolk 2 (del 1): Penger fikk inntekt + sparemål (nye stores `incomes` + `goals`, schema v8). Inntekt-kort på Oversikt-fanen + «igjen å bruke» i sammendraget. Ny «Sparing»-fane med sparemål (fremdriftslinje, + Spar/− for å justere spart, rediger mål/slett). Med i sync + backup.
- 2026-06-29 — Bolk 1 (del 3, ferdig): Idébank fikk frie tagger (#tag-chips på kortet, tag-editor i utvidet visning, klikk på tagg søker; søk matcher nå tagger). Liste-gjøremål fikk sjekkliste/delpunkter (`subtasks`-array + `addSubtask`/`toggleSubtask`/`deleteSubtask`, ☑ N/M-chip som utvider en mini-sjekkliste). Begge er uindekserte array-felter.
- 2026-06-29 — Bolk 1 (del 2): Vaner fikk emoji + farge + arkiv (uindekserte felt på `habits`, customize-panel per kort, fargede prikker, arkivseksjon med «hent tilbake»). Prosjekter fikk frist (deadline-dato med forfall-farge) og eget notatfelt (uindekserte felt på `projects`).
- 2026-06-29 — Bolk 1 (del 1) av funksjonsutrullingen: «Utsett til i morgen»-knapp (måne) på I dag-oppgaver (`snoozeTaskToTomorrow`), sort-toggle Manuelt/Dato + «→ I dag»-knapp på Liste-gjøremål, «Kommende betalinger»-liste på Penger/Faste (sortert på trekkdag), og en roterende dags-hilsen på Oversikt.
- 2026-06-29 — Byttet ut prosjekt-roadmappen (tidslinje Nå→Neste→Senere) med en prioritetsliste: samme data (stage now/next/later beholdt), men relabelet til Høy/Medium/Lav prioritet med fargeprikker, «Viktigst nå»-hero og oppdatert statistikk. Lav-risiko relabel/restyle, ingen schema-endring eller migrering.
- 2026-06-29 — Ryddet bunnmenyen på mobil: 4 faste faner (Oversikt, I dag, Kalender, Prosjekter) + «Mer»-knapp som åpner en sheet med resten (Liste, Hva nå?, Idébank, Vaner, Penger, Backup). PC-sidemenyen viser alt som før (nav-secondary vises, nav-more skjules via media query).
- 2026-06-29 — Bedre roadmap: steg kan nå flyttes fritt mellom faser (Nå/Neste/Senere), omrokeres opp/ned, fullføres eller slettes via en handlings-sheet (⋯ på hvert kort). Per-fase «Legg til» (legg direkte i Nå/Neste/Senere). Ferdig-kort er trykkbare for å angre/flytte tilbake. Nye db-funksjoner `moveItemToStage` + `reorderItem`; den gamle ett-veis `advanceItem`/pilen er fjernet.
- 2026-06-29 — Penger ble et budsjettsystem: nye stores `expenses` (logget forbruk) + `budgets` (månedsbudsjett per kategori), schema v7. Tre faner: Oversikt (måned-nav, total brukt vs budsjett, fargede stolper per kategori der spent = forbruk + abonnement), Forbruk (logg kjøp via bunn-sheet med kategori-rutenett), Faste (eksisterende abonnement). Unified `CATEGORIES` med emoji + farge på tvers av forbruk/budsjett/abonnement.
- 2026-06-29 — Prosjekter fikk «profil»: emoji + farge som identitet (uindekserte felt på `projects`, ingen schema-bump), fargevelger + emoji-rutenett i kustomiseringspanel. Profilsiden viser nå fremdrift i prosjektfarge + %, og statistikk-rad (Nå/Neste/Senere/Ferdig). Liste-kortene viser emoji-merke + fremdriftslinje.
- 2026-06-29 — Ny «Liste»-modul: enkel gjøremålsliste uten dato (ny `todos`-store, schema v6). Hak av (gnist), rediger med ett trykk, sorter, fullført-seksjon. Med i sync + backup. Flyttet `.sort-btn` til AppShell.css (delt med Prosjekter). Også: nav flyttet til bunnen på mobil, prosjektsteg redigerbare med ett trykk + redigerbart «Neste steg», e-post lowercases ved innlogging (fikset iPhone stor-forbokstav som lagde to kontoer).
- 2026-06-28 — Ny «Kalender»-modul: månedsvisning (mandag-først), hendelser med tid/farge/notat (ny `events`-store, schema v5), oppgaver vises på forfallsdato og kan hukes av i dag-agendaen, bunn-sheet for å legge til/redigere, swipe mellom måneder. Bunn-nav gjort horisontalt scrollbar (8 moduler). Verifisert med skjermbilder.
- 2026-06-28 — La til Dexie Cloud sync med e-post-innlogging. Egendefinert auth-GUI (todelt innloggingsskjerm + norsk e-post/kode-dialog) som matcher designet i stedet for Dexies standard-GUI. `requireAuth: false` + `customLoginGui: true`.
- 2026-06-28 — La til «Oversikt» (startside med live modul-kort) og «Hva nå?» (ett forslag av gangen, energifilter, hurtiglegg-til). Penger fikk kategorier + oversikt. Rediger/slett/sorter på tvers av moduler.
- 2026-06-28 — Committet og pushet «Prosjekter»-roadmapmodulen til main (oversikt + roadmap-side, projects + projectItems, WIP-tak 3, idébank→prosjekt, utvidet eksport/import).
- 2026-06-28 — Opprettet tre-fils dokumentasjonssystem (CLAUDE.md slanket til varige regler + nåtilstand, ROADMAP.md, PROGRESS.md) for å holde styring og historikk adskilt.
- 2026-06-28 — Bygde «Prosjekter» som roadmap-modul (nye stores `projects` + `projectItems`, hero «neste steg», ryggrad Nå→Neste→Senere, WIP-tak på 3 aktive, «Forfremm til prosjekt» fra idébanken). Migrerte gamle prosjekter til nytt schema. Ukommittert til browser-test er gjort.
- 2026-06-28 — Utvidet fra én skjerm til fullt dashboard: app-skall med responsiv navigasjon (sidemeny på PC, bunnfaner på mobil) + modulene «I dag», «Vaner» og «Penger». Backup utvidet til å dekke alle stores.
- 2026-06-28 — Bygde idébanken (Dexie lokal lagring, CRUD, søk/filter, gnist + vibrasjon, JSON eksport/import), satte opp PWA og deployet til Vercel.
